$('#items').append('<%= j render @friendships %>');
<% if @friendships.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @friendships %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>